node_modules
.env
dist
build
.next
coverage
apps/**/coverage

dev.db
*.db
scripts/
!scripts/dr-failover.ts
!scripts/generate-types.ts
!scripts/k6-load-test.js
!scripts/verify-migrations.ts
!scripts/verify-migrations.test.ts
!scripts/security-audit.ts
# Note: a `!scripts/<file>` negation cannot un-ignore a file inside an
# already-excluded parent directory (git limitation) — every negated/tracked
# one-off ops script above (k6-load-test.js included) is added with
# `git add -f`; the negation lines are kept only as a visible inventory.

apps/**/.env.local

target/
Cargo.lock
